describe('Array',function(){
    it('should same with JS',function(){

        const names: string[] = ["qwerty","putra"]
        const numbers: number[] = [1,2,3]

        console.info(names)
        console.info(numbers)
    })
})